# EmberSight — project notes for Claude

## Environment assumptions

- **`ANTHROPIC_API_KEY` is always set** in any environment Claude works in for this project. Do NOT list it as a missing dependency, a configuration step, or a gap when reporting on agent functionality. Treat all LLM-narrative paths as "live" for the purposes of QA reports.
